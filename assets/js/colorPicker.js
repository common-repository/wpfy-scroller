(function ($) {
  $(function () {
    // Add Color Picker to all inputs that have 'color-field' class
    $(".cpa-color-picker").wpColorPicker();
    $(".cpa-color-picker-hover").wpColorPicker();
  });
})(jQuery);
